//
//  ActivityTrackerApp.swift
//  ActivityTracker
//
//  Created by Charel Kremer on 28/01/2026.
//

import SwiftUI

/// NOTE:
/// The real application entry point is defined in `MainApp.swift` at the repository root (`ActivityTrackerApp`).
/// This placeholder is kept only so the Xcode-created files remain, but it is not marked with `@main`
/// and is not used by the running app.
struct TemplateActivityTrackerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
